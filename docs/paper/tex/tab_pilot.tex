% HAND-MAINTAINED. make_tex_tables.py has no tab_pilot() function, so this file is NOT
% generated despite what earlier revisions of this header claimed. Values are the
% Protocol-P crude-strategy probe (findings_log.md run 5) and are transcribed from
% make_figures.py's PROBE table. Do not confuse them with the rich-strategy bake-off
% in run_all_bakeoff.json (findings_log.md run 8), which scores the same 17 classes
% higher because it uses a different descriptor strategy.
\begin{table}[htbp]
\centering
\caption{Pilot encoder survey (Protocol~P: 17 held-out classes, chance 5.9\%, \emph{crude} single-sentence keyword descriptors --- not the \emph{rich} symptom bank used in the bake-off of Section~\ref{sec:bakeoff}, whose numbers are accordingly higher). All ten publicly available aligned encoders we could load with open\_clip are included; none is excluded. No association between accuracy and parameter count is detectable here (Spearman $\rho = 0.35$, $p = 0.32$); with ten encoders the test resolves only $|\rho| \gtrsim 0.65$, so this bounds the association rather than showing it is absent, and Section~\ref{sec:flat} reports a positive coefficient in all twelve cells of Table~\ref{tab:scale}.}
\label{tab:pilot}
\begin{tabular}{lrr}
\toprule
Encoder & Params (M) & Zero-shot top-1 (\%) \\
\midrule
MobileCLIP2-S0 & 11.4 & 19.9\\
MobileCLIP-S1 & 21.5 & 20.8\\
MobileCLIP-S2 & 35.8 & 17.7\\
MobileCLIP2-S2 & 35.8 & 20.3\\
MobileCLIP-B & 86.3 & 21.9\\
MobileCLIP2-B & 86.3 & 17.1\\
ViT-B-16-SigLIP2 & 92.9 & 25.6\\
MobileCLIP2-S3 & 125.2 & 19.5\\
MobileCLIP2-L-14 & 304.0 & 21.6\\
MobileCLIP2-S4 & 321.8 & 21.0\\
\bottomrule
\end{tabular}
\end{table}
