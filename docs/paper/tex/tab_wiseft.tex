% GENERATED by docs/paper/make_tex_tables.py -- do not edit by hand.
\begin{table}[htbp]
\centering
\caption{WiSE-FT weight ensembling on MobileCLIP2-S0: the seen/unseen trade-off as a single dial (166 seen classes, 55,981 images; 17 unseen classes; protocol: pilot unseen set with nested-C seen set -- MIXED PROTOCOLS). NOTE: legacy file, protocols differ between the two columns.}
\label{tab:wiseft}
\begin{tabular}{lrr}
\toprule
$\alpha$ & Seen & Unseen (zero-shot) \\
\midrule
0.0 (frozen) & 82.6\% & 17.0\% \\
0.50 & 87.7\% & 16.3\% \\
1.0 (naive fine-tune) & 90.3\% & 8.8\% \\
\bottomrule
\end{tabular}
\begin{flushleft}\footnotesize $\alpha=0$ reproduces the frozen baseline, validating the interpolation. $\alpha=1$ is naive fine-tuning and shows catastrophic forgetting, retaining only 1.5$\times$ chance on unseen crops. Seen accuracy rises monotonically with $\alpha$ while unseen accuracy falls, so the dial trades one for the other as intended; $\alpha=0.50$ maximises their sum.\end{flushleft}
\end{table}
