# Plant-Disease-Edge — Phase A dependencies.
# For the RTX 4060 (local), install PyTorch with CUDA FIRST, then this file:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# On Kaggle, torch/torchvision are preinstalled — just `pip install -q` the rest.

datasets>=2.18.0          # streaming SAGE from HuggingFace
huggingface_hub>=0.23.0
timm>=1.0.3               # EdgeNeXt student + DINOv2; needs >=1.0 for edgenext_xx_small.in1k
open_clip_torch>=2.24.0   # CLIP / OpenCLIP teachers
pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
# Descriptor generation (Phase A2) via Lava's OpenAI-compatible endpoint:
openai>=1.40.0
python-dotenv>=1.0.0
# Edge benchmark (Phase D) — INT8 export + on-device latency:
onnx>=1.16.0
onnxruntime>=1.18.0
# thop>=0.1.1              # optional, for MACs/GFLOPs in benchmark_edge.py
# Optional native Anthropic SDK (only if you use --provider anthropic instead of Lava):
# anthropic>=0.34.0
